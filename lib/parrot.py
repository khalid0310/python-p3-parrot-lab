
def parrot(message="Squawk!"):
    """
    Accepts a string as an argument, prints it, and returns it.

    Parameters:
    - message (str): The message to be printed and returned.

    Returns:
    - str: The same message that was passed as an argument.
    """
    print(message)
    return message